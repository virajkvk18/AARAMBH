# Configuration (AARAMBH)

- environment/ — environment configuration
- authentication/ — authentication configuration
- database/ — database configuration
- ai/ — ai configuration
- external-services/ — external-services configuration
